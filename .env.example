# Browser OAuth2 Authorization Code exchange for user-service.
# The supplied local Auth Server image works without this override.
# Set it only when using a different Auth Server registration.
AUTH_WEB_CLIENT_ID=web-client
AUTH_WEB_CLIENT_SECRET=
AUTH_WEB_REDIRECT_URI=http://localhost:3000/callback
FRONTEND_BASE_URL=http://localhost:3000
